import * as models from '../models/product';

class ProductCalculator {
    Calculate(data: models.Product[], pageSize: number, propertySelector: (product: models.Product) => number): number[] {
        return [];
    }
}

export default ProductCalculator;